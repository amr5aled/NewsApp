import 'package:flutter/material.dart';

class SettingNews extends StatelessWidget {
  const SettingNews({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('setting News'),
    );
  }
}